[tool.poetry]
name = "lessons"
version = "0.1.0"
description = ""
authors = ["xzerwsn <xzerwsn@mail.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
